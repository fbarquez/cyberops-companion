# =============================================================================
# ISORA - Production Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values
# Generate secrets with: openssl rand -hex 32
# =============================================================================

# Domain Configuration
DOMAIN=cyberops.yourdomain.com

# Database
DB_USER=cyberops
DB_PASSWORD=your-super-secure-db-password
DB_NAME=cyberops_companion

# Redis
REDIS_PASSWORD=your-redis-password

# Security Keys (generate with: openssl rand -hex 32)
JWT_SECRET=your-jwt-secret-key-change-this
SECRET_KEY=your-secret-key-change-this

# Token Expiration
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# AI Copilot Configuration
# Options: ollama, openai, anthropic, groq, gemini
COPILOT_PROVIDER=openai
COPILOT_MODEL=gpt-4o-mini

# API Keys (only needed for cloud AI providers)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
# GROQ_API_KEY=gsk_...
# GOOGLE_API_KEY=...

# For self-hosted Ollama (optional)
# OLLAMA_URL=http://ollama:11434

# =============================================================================
# Quick Start Checklist:
# =============================================================================
# [ ] Set your DOMAIN
# [ ] Generate DB_PASSWORD: openssl rand -base64 24
# [ ] Generate REDIS_PASSWORD: openssl rand -base64 24
# [ ] Generate JWT_SECRET: openssl rand -hex 32
# [ ] Generate SECRET_KEY: openssl rand -hex 32
# [ ] Add at least one AI API key (OPENAI_API_KEY recommended)
# =============================================================================
